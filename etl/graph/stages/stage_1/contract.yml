artifacts:
  - path: compiled/taxa.jsonl
    type: jsonl
    validators:
      - kind: field_presence
        fields: ["id", "rank", "display_name", "latin_name", "parent"]
      - kind: id_format_consistency
        field: id
        pattern: "^tx:[paf]:"
