artifacts:
  - path: compiled/taxa.jsonl
    type: jsonl
    validators:
      - kind: min_rows
        min_rows: 1
      - kind: field_presence
        fields: ["id", "rank", "display_name", "latin_name", "parent"]
      - kind: field_format
        field: id
        pattern: "^tx:[paf]:"
