{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true
    },
    "build": {
      "dependsOn": [
        "^build"
      ],
      "outputs": [
        "dist/**"
      ]
    },
    "lint": {
      "outputs": []
    },
    "typecheck": {
      "outputs": []
    },
    "etl:build": {
      "outputs": [
        "etl/dist/**",
        "data/ontology/compiled/**"
      ]
    },
    "etl:validate": {
      "outputs": []
    },
    "etl:clean": {
      "outputs": []
    },
    "etl2:run": {
      "inputs": [
        "etl2/**",
        "data/ontology/**"
      ],
      "outputs": [
        "etl2/build/**"
      ]
    }
  },
  "globalDependencies": [
    "**/.env*"
  ]
}